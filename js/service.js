'use strict'

function setShape(shape) {
    gCurrShape = shape
}
function setColor(color) {
    gColor = color;
}
function setCanvasColor(color) {
    gCanvasColor=color
}
function setBrushSize(size) {
    gCurrBrushSize=size
}
function startDrawing(isDrawing) {
    gIsDrawing = isDrawing
}